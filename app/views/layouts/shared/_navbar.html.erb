<ul class="flex">
  <% if user_signed_in? %>
    <li class="mr-3">
      <%= link_to "Logout", destroy_user_session_path, class: "inline-block border border-blue-500 rounded py-1 px-3 bg-blue-500 text-white", data: { turbo_method: "delete" } %>
    </li>
  <% end %>
</ul>
